package com.greenq.repository;

import com.greenq.entity.StandardSet;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StandardSetRepository extends JpaRepository<StandardSet, Long> {
}
